export default function HomePage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-24">
      <h1 className="text-2xl font-semibold">The R — Profit Management</h1>
      <p className="mt-4 text-slate-600">
        Phase 0 scaffold. Sign-in (Microsoft SSO), the portfolio dashboard, and
        cost entry land in Phase 1 — see /docs in the repo for the full plan.
      </p>
    </main>
  );
}
