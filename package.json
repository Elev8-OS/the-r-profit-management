{
  "name": "the-r-profit-management",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "db:generate": "pnpm --filter @the-r/db exec prisma generate",
    "db:migrate": "pnpm --filter @the-r/db exec prisma migrate deploy"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.7.0"
}
